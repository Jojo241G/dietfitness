package com.dietfitness; // Correction du package racine

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BackendApplicationTests {

    @Test
    void contextLoads() {
        // Test de fumée (Smoke test) qui vérifie que le contexte Spring Boot démarre sans crash
    }
}